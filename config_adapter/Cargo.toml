[package]
name = "config_adapter"
version = "0.1.0"
edition = "2021"
description = "Configuration persistence adapter using confy"

[dependencies]
core_domain = { path = "../core_domain" }
confy = "0.6.1"
serde = { version = "1.0", features = ["derive"] }
notify = "6.1.1"
